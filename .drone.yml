workspace:
  base: /go/src
  path: github.com/minchao/shurara

pipeline:
  server:
    group: build
    image: golang
    commands:
      - make build

  webapp:
    group: build
    image: node:6.11
    commands:
      - cd ./webapp
      - make lint
      - make build